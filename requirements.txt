# Author: Maharshi Soni | License: MIT
# Core dependencies (no paid APIs required)
sentence-transformers==2.2.2
torch==2.1.2
numpy==1.26.2

# Testing
pytest==7.4.4
pytest-cov==4.1.0

# Optional: for enhanced similarity (falls back to built-in TF-IDF if not installed)
# scikit-learn==1.3.2
